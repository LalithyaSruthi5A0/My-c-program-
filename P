#include<stdio.h>
void main()
{
int num, last_digit,r_d;
printf("enter number:");scanf("%d", &num);
int n=num;
for(result=0;num>0;num=num/10)
{
rem=num%10;
result=result*10+rem;
}
if(result==n)
printf("%d is polindrome",n);
else
printf("%d is not a polindrom",n);
}
